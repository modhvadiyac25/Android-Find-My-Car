<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.findmycar">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".EmiCalculator">

        </activity>
        <activity android:name=".DetailActivity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".NewsFeed">

        </activity>

        <activity android:name=".PopularCars" />
        <activity android:name=".CarDetails" />
        <activity android:name=".SearchList" />
        <activity android:name=".Search" />
        <activity android:name=".UploadPic" />

        <activity android:name=".test" />
        <activity android:name=".UserProfile" />
        <activity android:name=".UpdateProfile" />
 
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
 
        <activity android:name=".LoginActivity"> 
            <!-- if you want to start your app with main activity then cut this <intent-filter> tag and past it in main activity -->

        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>